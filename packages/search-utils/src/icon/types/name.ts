import type { IconifyIconName } from '@iconify/utils';

/**
 * Icon name
 *
 * Contains name as both object and string
 */
export interface IconFinderIconName extends IconifyIconName {
	// Name as string
	value: string;
}
