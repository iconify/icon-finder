import type { IconFinderCollectionsListItem } from './collections';

/**
 * Callback for filter function
 */
export type FilterCollectionsListCallback = (
	item: IconFinderCollectionsListItem
) => boolean;
